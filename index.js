const returnFirstTwoDrivers = function(drivers) {
  let firstDrivers = [drivers[0], drivers[1]];
  return firstDrivers;
};

const returnLastTwoDrivers = function(drivers) {
  let lastDrivers = [drivers[-1], drivers[-2]];
  return lastDrivers;
};